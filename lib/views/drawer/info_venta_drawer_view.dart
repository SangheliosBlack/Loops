import 'package:flutter/material.dart';

class InfoVendedoresView extends StatelessWidget {
  const InfoVendedoresView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mis pedidos'),
      ),
    );
  }
}
